import { createHeader, createLayout} from "./layout.js";

createHeader();

const {
    addTaskInput,
    addTaskBtn,
    allTasksCountBadge,
    doneTasksCountBadge,
    emptyStorage,
    form,
    storageContainer
  } = createLayout();
  

