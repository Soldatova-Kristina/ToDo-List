import { createLayout } from "./layout";
const {
    addTaskInput,
    addTaskBtn,
    allTasksCountBadge,
    doneTasksCountBadge,
    emptyStorage,
    form,
    storageContainer
  } = createLayout();
  
addTaskInput.addEventListener("submit", (e) => {
    
}) 